version: '3.8'

services:
  domain-agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: domain_intelligence_agent
    ports:
      - "8501:8501"
      - "3000:3000"
    environment:
      - PORT=3000
      - RATE_LIMIT_DELAY=2
    restart: unless-stopped
